<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.button :submit %>
  <br>
  <%= link_to "Show all cocktails", cocktails_path%>
<% end %>
